# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=gotradex

# Connection Strings (Production)
# Use service names inside Docker, or external IPs if hosted separately
DATABASE_URL=postgres://postgres:postgres@postgres:5432/gotradex?sslmode=disable
KAFKA_BROKERS=kafka:9092

# Image Configuration
IMAGE_TAG=latest
GITHUB_REPOSITORY_OWNER=your-username
